{
  "name": "@fastloop/panel",
  "version": "0.1.1-beta.8",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node ../scripts/validate-panel-dist.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "vite"
  },
  "dependencies": {
    "@fastloop/shared": "0.1.1-beta.8"
  }
}
